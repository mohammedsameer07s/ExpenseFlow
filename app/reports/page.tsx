"use client"; import AppShell from "@/components/AppShell"; import {load} from "@/lib/store"; import {useEffect,useMemo,useState} from "react"; import {BarChart,Bar,XAxis,YAxis,Tooltip,ResponsiveContainer,PieChart,Pie,Cell} from "recharts";
export default function Page(){const[d,setD]=useState<any>();useEffect(()=>setD(load()),[]);const data=useMemo(()=>{if(!d)return[];const m:any={};d.transactions.filter((x:any)=>x.type==="expense").forEach((x:any)=>m[x.category]=(m[x.category]||0)+x.amount);return Object.entries(m).map(([category,amount])=>({category,amount}))},[d]);if(!d)return null;return <AppShell><div className="topbar"><div><div className="muted">Understand your habits</div><h1>Reports & Analytics</h1></div></div><div className="layout2"><section className="card section-card" style={{height:390}}><h2>Spending by Category</h2><ResponsiveContainer width="100%" height="90%"><BarChart data={data as any}><XAxis dataKey="category" stroke="#9da5b8"/><YAxis stroke="#9da5b8"/><Tooltip/><Bar dataKey="amount" fill="#6d7cff"/></BarChart></ResponsiveContainer></section><section className="card section-card" style={{height:390}}><h2>Category Mix</h2><ResponsiveContainer width="100%" height="90%"><PieChart><Pie data={data as any} dataKey="amount" nameKey="category" cx="50%" cy="50%" outerRadius={110} label>{data.map((_,i)=><Cell key={i} fill={["#6d7cff","#9c6cff","#42d392","#ffc857","#ff6b81"][i%5]}/>)}</Pie><Tooltip/></PieChart></ResponsiveContainer></section></div><div className="card section-card" style={{marginTop:16}}><h2>Rule-based Insights</h2><div className="chips"><span className="chip">Track recurring subscriptions</span><span className="chip">Review high-spend categories</span><span className="chip">Set a monthly savings target</span><span className="chip">Keep emergency savings growing</span></div></div></AppShell>}